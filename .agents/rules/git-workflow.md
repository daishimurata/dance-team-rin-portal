# Git 運用ルール（自動同期ワークフロー）

本プロジェクト（`dance-team-rin-portal`）では、作業を行う際、以下のルールを厳守すること。

## ルール
1. **作業開始前**: 編集を開始する前に、必ず `git pull origin main` を実行してリモートリポジトリの最新コードを取得・同期すること。
2. **作業完了時**: 編集・変更作業が完了したら、必ず変更内容をコミット（`git commit`）し、リモートリポジトリ（GitHub）へプッシュ（`git push origin main`）すること。
